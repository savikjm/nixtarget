$(document).ready(function () {
    //var $table = $('#companies_table');
    //$table.bootstrapTable();

    //$table.bootstrapTable({
    //}).on('click-row.bs.table', function (e, row, $element) {
    //    window.location.href = "/Authenticated/CompanyDetails.aspx?id=" + row[0];
    //});
});